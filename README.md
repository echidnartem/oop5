# Объектно-ориентированное программирование

Этот репозиторий содержит лабораторную работу по курсу «Объектно-ориентированное программирование» (ООП) Московского авиационного института (МАИ), 2 курс, 8 факультет.

## 📚 Лабораторная работа

### Лабораторная работа 6: Итераторы и аллокаторы
**Задание (вариант 6)**:
- Реализовать потомок класса `std::pmr::memory_resource`, реализующий стратегию управления памятью согласно варианту задания.
- `memory_resource` должен позволять повторное использование освобождённой памяти (ранее освобождённой с помощью `do_deallocate`).
- При уничтожении объекта `memory_resource` должен освободить всю память, которая не была освобождена ранее.
- Реализовать шаблонный контейнер (динамический массив) согласно варианту задания, использующий созданный `memory_resource` через шаблон `std::pmr::polymorphic_allocator`.
- Реализовать итератор для созданного контейнера. Итератор должен удовлетворять требованиям `std::forward_iterator_tag`.
- Написать код, демонстрирующий работу контейнера с простыми (`int`) и составными типами (структурами с несколькими полями).
- Стратегия выделения памяти: для каждого объекта выделяется отдельный блок памяти на куче; информация о выделенных блоках хранится в `std::map`.

## 👨‍🎓 Студент
**ФИО:** Мицкевич Артём Алексеевич. \
**Группа:** М8О-211БВ-24.
